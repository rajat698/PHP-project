version: '3'
services:
  www:
    image: php:apache
    volumes:
      - "./:/var/www/html"
    ports:
      - 80:80
      - 443:443
  
  db:
    image: mysql:latest
    environment:
      # - MYSQL_DATABASE=php_docker
      - MYSQL_USER=emailofrajatyadav@gmail.com
      - MYSQL_PASSWORD=Password12.
      - MYSQL_ALLOWEMPTY_PASSWORD=1

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports: 
    - 8001:80
    environment:
      - PMA_HOST=db
      - PMA_PORT=3300
    

